export async function getAllTags(): Promise<string[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is retrieving all unique tags used across clipboard entries.
    // It should return a sorted array of unique tag strings.
    return [];
}

export async function getPopularTags(limit: number = 10): Promise<Array<{tag: string, count: number}>> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is retrieving the most popular tags with their usage counts.
    // It should return tags sorted by usage frequency in descending order.
    return [];
}